apiVersion: v1
kind: Service
metadata:
  name: spring-boot-app-service 
spec:
  selector:
    app: sp-app
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080  
  type: NodePort
